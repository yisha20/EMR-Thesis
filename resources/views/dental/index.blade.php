@extends('layouts.app') @section('content')<div class="container"><h1>Dental Referrals</h1>@foreach($referrals as $r)<a class="card card-body mb-2" href="{{route('dental-referrals.show',$r)}}"><strong>{{$r->patient->first_name}} {{$r->patient->last_name}}</strong><span>{{$r->dental_concern}} · {{ucfirst($r->triage_priority)}} · {{str_replace('_',' ',$r->status)}}</span></a>@endforeach {{$referrals->links()}}</div>@endsection
